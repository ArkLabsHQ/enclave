.vscode
build/
bin/
# Nix build outputs
enclave/artifacts/
flake_result
flake_result-*
enclave-image.tar.gz

# Generated build config (from enclave.yaml for Nix)
enclave/build-config.json

# OpenTofu
enclave/tofu/.terraform/
enclave/tofu/.terraform.lock.hcl
enclave/tofu/terraform.tfstate*
enclave/tofu/terraform.tfvars.json
enclave/tofu/tofu-outputs.json
enclave/tofu/backend.tf
enclave/tofu/provider_override.tf

# Test logs
test/tofu-*.log

# Enclave CLI config (contains account IDs)
enclave.yaml


# Built binaries
introspector
introspector-client
synth-local
./bin/
*.eif
supervisor/supervisor

# Temporary build artifacts
/tmp/enclave-verify-*

# Go vendor directory
vendor/

# Rust build output
client-rs/target/

# TypeScript build output
client-ts/node_modules/
client-ts/dist/

# Example folder (not used in production) /examples/
example/

bin/

/target/
.claude/