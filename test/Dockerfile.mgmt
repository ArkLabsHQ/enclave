FROM golang:1.25-alpine AS builder
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -trimpath -o /enclave-mgmt ./mgmt/

FROM alpine:latest
RUN apk add --no-cache ca-certificates
COPY --from=builder /enclave-mgmt /usr/local/bin/
ENTRYPOINT ["enclave-mgmt"]
