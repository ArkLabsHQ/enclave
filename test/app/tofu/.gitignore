# OpenTofu state and outputs (contains account-specific IDs)
tofu-outputs.json
terraform.tfvars.json
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl
backend.tf
provider_override.tf
modules/enclave/.signing/

# CLI-managed scaffold — regenerated by `enclave tofu init` on every test run.
# Don't commit; the framework's templates are the source of truth.
main.tf
modules/enclave/main.tf

# CLI-managed env overrides — regenerated by `enclave tofu env` calls inside
# the test scripts.
env_values.auto.tfvars.json
